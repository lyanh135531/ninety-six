generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
}

model Category {
  id        String    @id @default(cuid())
  name      String
  slug      String    @unique
  products  Product[]
  
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model Product {
  id          String   @id @default(cuid())
  name        String
  slug        String   @unique
  description String?
  price       Float
  imageUrl    String?
  isFeatured  Boolean  @default(false)
  sizes        String?  // Comma-separated sizes like "S,M,L,XL"
  stockBySizes String?  // JSON: {"S":10,"M":0,"L":5} or {"_total":30} for no-size products
  
  categoryId  String
  category    Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Order {
  id              String   @id @default(cuid())
  customerName    String
  customerPhone   String
  customerAddress String
  totalAmount     Float
  paymentMethod   String   // "COD" or "BANK_TRANSFER"
  status          String   @default("PENDING") // PENDING, PROCESSING, COMPLETED, CANCELLED
  orderItems      String   // JSON stringified order details for MVP
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
